<Window x:Class="ClipboardManager.Wpf.Views.SidebarTabWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="36" Height="110"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        Cursor="Hand">

    <Border x:Name="TabBorder" Background="White" CornerRadius="8 0 0 8"
            BorderBrush="#DEE2E6" BorderThickness="1 1 0 1"
            Padding="0">
        <Border.Effect>
            <DropShadowEffect BlurRadius="8" ShadowDepth="2"
                              Opacity="0.12" Direction="270"/>
        </Border.Effect>

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <!-- 图标 -->
            <Border Width="28" Height="28" CornerRadius="6"
                    Background="#E8F4FD" Margin="0,0,0,6">
                <TextBlock Text="📋" FontSize="14"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Border>
            <!-- 文字 -->
            <TextBlock Text="剪" FontSize="10" Foreground="#999999"
                       HorizontalAlignment="Center" FontWeight="SemiBold"/>
        </StackPanel>
    </Border>

    <!-- 切换颜色背景（鼠标悬停时变浅蓝） -->
    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.MouseEnter">
            <BeginStoryboard>
                <Storyboard>
                    <ColorAnimation Storyboard.TargetName="TabBorder"
                                     Storyboard.TargetProperty="Background.Color"
                                     To="#F0F7FF" Duration="0:0:0.15"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
        <EventTrigger RoutedEvent="Window.MouseLeave">
            <BeginStoryboard>
                <Storyboard>
                    <ColorAnimation Storyboard.TargetName="TabBorder"
                                     Storyboard.TargetProperty="Background.Color"
                                     To="#FFFFFF" Duration="0:0:0.15"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>
</Window>
